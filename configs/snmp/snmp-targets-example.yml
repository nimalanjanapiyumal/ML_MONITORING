# Example SNMP target list for documentation.
# Use this with Prometheus snmp_exporter after generating a full snmp.yml file.

targets:
  - name: lab-router
    ip: 192.168.1.1
    module: if_mib
    community: public
  - name: lab-switch
    ip: 192.168.1.2
    module: if_mib
    community: public
